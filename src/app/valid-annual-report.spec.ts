import { ValidAnnualReport } from './valid-annual-report';

describe('ValidAnnualReport', () => {
  it('should create an instance', () => {
    expect(new ValidAnnualReport()).toBeTruthy();
  });
});
